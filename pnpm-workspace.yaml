packages:
  - apps/*
  - packages/*
# workerd downloads its binary in a postinstall script; wrangler/vite-plugin bundle with esbuild.
allowBuilds:
  esbuild: true
  workerd: true
# mermaid's parser (chevrotain) pins a lodash-es with published advisories.
overrides:
  lodash-es@<4.17.24: ^4.18.1
